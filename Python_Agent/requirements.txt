# Python dependencies for the AI Agent (agent + bridge + providers).
# Install with:  py -m pip install -r requirements.txt
# Versions pinned to the environment the plugin is developed against.

pydantic==2.12.5
google-genai==1.65.0
httpx==0.28.1
groq==1.0.0
ollama==0.6.1
python-dotenv==1.2.2
