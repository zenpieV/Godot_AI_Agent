extends Node

# Test fixture for the find_nodes_by_script headless
# harness: a second, distinct script so the harness can
# prove that nodes are matched by their actual attached
# script resource, not by class or name.

const FIXTURE_TAG := "enemy_script"
